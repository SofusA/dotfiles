[[language]]
name = "c-sharp"
auto-format = true

[[language]]
name = "typescript"
formatter = { command = 'prettier', args = ["--write", "--parser", "typescript", "--print-width", "120", "--single-quote", "false", "--html-whitespace-sensitivity", "ignore"] }
auto-format = true

[[language]]
name = "html"
formatter = { command = 'prettier', args = ["--write", "--parser", "html"] }
auto-format = true

[[language]]
name = "json"
formatter = { command = 'prettier', args = ["--write", "--parser", "json"] }
auto-format = true

[[language]]
name = "css"
formatter = { command = 'prettier', args = ["--write", "--parser", "css"] }
auto-format = true

[[language]]
name = "scss"
formatter = { command = 'prettier', args = ["--write", "--parser", "scss"] }
auto-format = true
