[[language]]
name = "c-sharp"
auto-format = true

[[language]]
name = "typescript"
formatter = { command = 'prettier', args = ["--write", "--parser", "typescript"] }
auto-format = true

[[language]]
name = "html"
formatter = { command = 'prettier', args = ["--write", "--html-whitespace-sensitivity", "ignore", "--parser", "html"] }
auto-format = true

[[language]]
name = "json"
formatter = { command = 'prettier', args = ["--write", "--parser", "json"] }

[[language]]
name = "css"
formatter = { command = 'prettier', args = ["--write", "--parser", "css"] }
