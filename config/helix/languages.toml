# [language-server.omnisharp]
# command = "omnisharp"
# args = ["--languageserver"]

# [language-server.angular]
# command = "ngserver"
# args = ["--stdio", "--tsProbeLocations", "/usr/lib/node_modules", "--ngProbeLocations", "/usr/lib/node_modules"]

[[language]]
name = "c-sharp"
formatter = { command = "dotnet-csharpier" }
auto-format = true

[[language]]
name = "typescript"
formatter = { command = 'prettier', args = ["--write", "--parser", "typescript", "--trailing-comma", "all" ] }
auto-format = true
# language-servers = ["typescript-language-server", "angular"]

[[language]]
name = "html"
formatter = { command = 'prettier', args = ["--write", "--parser", "html"] }
auto-format = true
# language-servers = ["vscode-html-language-server", "angular"]

[[language]]
name = "json"
formatter = { command = 'prettier', args = ["--write", "--parser", "json"] }
auto-format = true

[[language]]
name = "css"
formatter = { command = 'prettier', args = ["--write", "--parser", "css"] }
auto-format = true

[[language]]
name = "scss"
formatter = { command = 'prettier', args = ["--write", "--parser", "scss"] }
auto-format = true
